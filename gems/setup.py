
from setuptools import setup, find_packages
setup(
    name = 'CustomSQLGems',
    version = '0.0.1',
    packages = ['CustomSQLGems'],
    package_dir = {'CustomSQLGems': '.'},
    description = '',
    install_requires = [],
)

